# BlazorAppSql

Referente a um projeto de estudo para o desenvolvimento de um app simples em Blazor Server Side com acesso a banco de dados SQL Server.

Incorporando os dois em um único container Docker, utilizando o **Docker Orchestration** para subir os dois containers e o Docker Compose para gerenciar os containers.